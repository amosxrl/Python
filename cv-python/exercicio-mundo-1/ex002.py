# raw_input() é a função utilizada para ler dados do teclado no Python 2.x. No Python 3.x, a função equivalente é input(). 
# O código abaixo é para Python 2.x.
nome = str(input('Digite seu nome: '))
print('É um prazer te conhecer {}!'.format(nome))
